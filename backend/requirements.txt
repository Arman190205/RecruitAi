# FastAPI + AI Recruitment Platform — Backend Dependencies

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Pydantic settings (reads .env)
pydantic-settings==2.6.1

# Groq SDK (OpenAI-compatible)
groq==0.13.0

# PDF parsing
pdfplumber==0.11.4

# DOCX parsing
python-docx==1.1.2

# Multipart file uploads
python-multipart==0.0.12

# Async HTTP (optional, for future use)
httpx==0.27.2

# Optional: enables TRUE semantic skill matching via embeddings in skill_matcher.py.
# Heavy (~400 MB with torch). If absent, the matcher automatically falls back to
# deterministic fuzzy matching (synonyms + token overlap) — no code change needed.
# NOTE: Render's free tier (512 MB RAM) may OOM with this installed; enable on a
# paid tier or run locally for the embeddings demo.
# sentence-transformers==3.3.1

# Testing
pytest==8.3.3
